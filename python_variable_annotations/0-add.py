#!/usr/bin/env python3
"Add two floating-point numbers and return their sum."


def add(a: float, b: float) -> float:
    """
    This function takes two float arguments and returns their sum as a float.
    If integer arguments are provided,
    they will be implicitly converted to floats.

    """
    return a + b
