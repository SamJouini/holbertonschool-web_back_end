#!/usr/bin/env python3
'Calculate and return the floor of a given float number.'

import math


def floor(n: float) -> int:
    """The floor of a number is the largest integer
    less than or equal to the number."""
    return math.floor(n)
