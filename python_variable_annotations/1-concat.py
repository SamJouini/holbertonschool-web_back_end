#!/usr/bin/env python3
"Concatenate two strings and return the result."


def concat(str1: str, str2: str) -> str:
    """This function takes two string arguments and returns a new string
    that is the concatenation of the input strings. The original strings
    are not modified."""
    return str1 + str2
